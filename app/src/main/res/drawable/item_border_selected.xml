<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Keep the inside transparent -->
    <corners android:radius="8dp" /> <!-- Match your CardView's cornerRadius -->
    <stroke android:width="2dp"
        android:color="@color/blue" /> <!-- Your desired blue color -->
</shape>