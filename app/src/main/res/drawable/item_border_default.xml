<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Or your CardView's default background -->
    <corners android:radius="8dp" /> <!-- Match your CardView's cornerRadius -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- A subtle border color or transparent -->
</shape>